/**
 * All animations can be referenced by starting orientation / movement / ending orientation
 * Orientation is forwards then down
 *
 *       +X     +Z      +Y
 *        \     |      /
 *          \   |    /
 *            origin
 *
 * For example mantling on the top of a building can be
 * '+Z+X/+Z+X/+X-Z'
 * And jumping could be
 * '+Y-Z/+2Y/+Y+Z'
 */

export const assetMap = {
    "": "",
};
